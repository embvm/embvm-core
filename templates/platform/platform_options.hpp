#ifndef TEMPLATE_PLATFORM_OPTIONS_HPP_
#define TEMPLATE_PLATFORM_OPTIONS_HPP_

/** TODO for new platforms:
 * Rename TEMPLATE_PLATFORM_OPTIONS_HPP_ to something different
 * Supply the proper driver registry definition
 * Add any other platform-related generic definitions here
 */

#include <driver/driver_registry.hpp>

/// Supply the definition for PlatformDriverRegistry. Keep that name the same.
// using PlatformDriverRegistry = embvm::StaticDriverRegistry<12>;
// using PlatformDriverRegistry = embvm::DynamicDriverRegistry;

#endif // TEMPLATE_PLATFORM_OPTIONS_HPP_
